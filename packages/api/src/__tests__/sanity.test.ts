import { describe, expect, it } from "vitest";

/**
 * Simple sanity test to verify the testing infrastructure works.
 * This test can be replaced with actual tests as the codebase evolves.
 */
describe("API Package", () => {
  it("should have a working test setup", () => {
    expect(true).toBe(true);
  });

  it("should perform basic math correctly", () => {
    expect(1 + 1).toBe(2);
  });
});
